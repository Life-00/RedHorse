# ============================================================================
# 데이터베이스 설정 (PostgreSQL RDS)
# ============================================================================
# RDS PostgreSQL 인스턴스 엔드포인트
DB_HOST=your-rds-endpoint.amazonaws.com
# PostgreSQL 포트 (기본값: 5432)
DB_PORT=5432
# 데이터베이스 이름
DB_NAME=rhythm_fairy
# 데이터베이스 사용자명
DB_USER=postgres
# 데이터베이스 비밀번호 (보안 주의!)
DB_PASSWORD=your-secure-password

# ============================================================================
# AWS 설정
# ============================================================================
# AWS 리전 (Lambda, RDS, S3, Bedrock 등이 위치한 리전)
AWS_REGION=us-east-1
# S3 버킷 이름 (파일 저장용)
S3_BUCKET_NAME=your-s3-bucket-name

# ============================================================================
# Cognito 설정 (사용자 인증)
# ============================================================================
# Cognito User Pool ID
COGNITO_USER_POOL_ID=your-user-pool-id
# Cognito App Client ID
COGNITO_CLIENT_ID=your-client-id

# ============================================================================
# API 설정
# ============================================================================
# API Gateway 스테이지 (dev, prod 등)
API_STAGE=dev
# CORS 허용 오리진 (프론트엔드 URL)
CORS_ORIGINS=http://localhost:5173

# ============================================================================
# Bedrock Agent 설정 (AI 기반 수면/카페인 추천)
# ============================================================================
# Bedrock Agent ID (ShiftSync-Bio-Coach)
# - 사용자의 근무 스케줄을 기반으로 맞춤형 수면 및 카페인 가이드 제공
BEDROCK_AGENT_ID=your-agent-id
# Bedrock Agent Alias ID
BEDROCK_AGENT_ALIAS_ID=your-alias-id
# Bedrock 서비스 리전
BEDROCK_REGION=us-east-1

# Bedrock OCR Agent 설정 (근무표 이미지 인식)
# - 근무표 이미지를 분석하여 스케줄 데이터 추출
BEDROCK_OCR_AGENT_ID=your-ocr-agent-id
BEDROCK_OCR_AGENT_ALIAS_ID=your-ocr-alias-id

# OCR Lambda 함수명
OCR_LAMBDA_NAME=ShiftSync-Vision-OCR

# ============================================================================
# 보안 그룹 ID (Lambda 배포 시 VPC 설정용)
# ============================================================================
# Lambda 함수용 보안 그룹 ID
# - Lambda가 VPC 내에서 RDS에 접근하기 위해 필요
LAMBDA_SECURITY_GROUP_ID=your-lambda-security-group-id
# RDS 인스턴스용 보안 그룹 ID
# - Lambda의 트래픽을 허용하도록 설정 필요
RDS_SECURITY_GROUP_ID=your-rds-security-group-id

# ============================================================================
# 기타 설정
# ============================================================================
# 로컬 개발 서버 포트
PORT=8000